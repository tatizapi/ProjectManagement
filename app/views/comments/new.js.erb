var add_comment_form_id = '#add-comment-form' + <%= @task.id %>

if ($(add_comment_form_id).is(':visible')) {
    $(add_comment_form_id).slideUp(350);
} else {
  $(add_comment_form_id).html("<%= j (render 'add_form') %>");
  $(add_comment_form_id).slideDown(350);
}
