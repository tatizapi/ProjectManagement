var new_comment_id = '#new-comment-' + <%= @ticket.id %>

if ($(new_comment_id).is(':visible')) {
    $(new_comment_id).slideUp(350);
} else {
  $(new_comment_id).html("<%= j (render 'form') %>");
  $(new_comment_id).slideDown(350);
}
