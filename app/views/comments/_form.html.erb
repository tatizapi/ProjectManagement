<%= form_with model: [@task, @comment] do |form| %>
  <%= form.text_area :body %><br>
  <% if @comment.attachments %>
    <% @comment.attachments.each do |attachment| %>
      <%= link_to comment_delete_attachment_path(@comment.task, @comment, attachment), method: :delete, remote: true, data: { confirm: 'Are you sure?' }, title: 'Delete attachment' do %>
        <i class="glyphicon glyphicon-remove"></i>
      <% end %>
      <%= link_to attachment.filename, attachment.file.url, target: :_blank %><br>
    <% end %>
  <% end %>
  <%= form.file_field :files, multiple: true %><br>
  <%= form.submit "Submit" %>
<% end %>

<script>

</script>
