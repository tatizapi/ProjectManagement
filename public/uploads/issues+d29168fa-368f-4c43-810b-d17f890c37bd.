- daca se uploadeaza doua imagini cu acelasi nume, in public/uploads a doua imagine salvata o va suprascrie pe prima
- cand se da pe edit si se aleg noi attachmenturi, cele de dinainte se sterg
- cand admin vede toti clientii, poate sa vada el si parolele lor? *


<%= form_with :url => { :controller => "projects", :action => "add_developers" } do |form| %>
  <h3>Add more developers</h3>
 <%= form.collection_check_boxes :developer_ids, @available_employees, :id, :full_name,
        { include_hidden: false } do |employee| %>
    <div class="collection-check-box">
      <%= employee.check_box %>
      <%= employee.label %>
    </div>
  <% end %><br>
  <%= form.submit("Add") %>
<% end %>

<%= form_with :url => { :controller => "projects", :action => "add_testers" } do |form| %>
  <h3>Add more testers</h3>

  <%= form.collection_check_boxes :tester_ids, @available_employees, :id, :full_name,
        { include_hidden: false } do |employee| %>
    <div class="collection-check-box">
      <%= employee.check_box %>
      <%= employee.label %>
    </div>
  <% end %><br>

  <%= form.submit("Add") %>
<% end %>

<%end%>


  <% @employees.each do |employee| %>
    <div class="collection-check-box">
      <% if employee.is_developer?(@project) %>
        <%= form.check_box :developers, checked: true %>
      <% else %>
        <%= form.check_box :developers %>
      <% end %>
      <%= form.label employee.full_name %>
    </div>
  <% end %>
  <%= form.submit("Save") %>

     <%= link_to change_status_path(@project), method: :post, class: 'btn btn-danger', title: 'Finish' do %>
       <i class="glyphicon glyphicon-chevron-right"></i>
     <% end %>


  <% comment.attachments.each do |attachment| %>
    <% unless attachment.is_image %>
      <%= link_to attachment.file.identifier, attachment.url, target: :_blank %><br>
    <% end %>
  <% end %>

    <%= video_tag(video.url, controls: true, class: "comment-video") %>
